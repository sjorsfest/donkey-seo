# Agent Rules

## Required After Coding
- After any code change, always run `make typecheck`.
- If typecheck fails, fix the issues and rerun `make typecheck` until it passes.
- In the final response, report whether `make typecheck` passed or failed.
